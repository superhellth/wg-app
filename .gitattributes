# Shell scripts must keep LF endings or the container shebang (#!/bin/sh) breaks
# when checked out on Windows. Same for the Pi display daemon.
*.sh text eol=lf
backup/backup.sh text eol=lf
pi/display/*.py text eol=lf
