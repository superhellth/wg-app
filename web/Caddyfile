# Cloudflare Tunnel terminates public TLS, so Caddy serves plain HTTP on :80.
:80 {
	root * /srv
	encode gzip

	handle /api/* {
		reverse_proxy api:3000
	}

	handle {
		try_files {path} /index.html
		file_server
	}
}
